# Asset base URL (build-time). Empty = same origin (e.g. local dev).
# Set to CDN URL for production (e.g. https://cdn.example.com).
# Next.js bakes this into the bundle at build.
NEXT_PUBLIC_ASSET_BASE_URL=
